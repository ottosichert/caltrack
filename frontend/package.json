{
  "name": "caltrack",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "query-string": "^6.9.0",
    "react": "^16.12.0",
    "react-dom": "^16.12.0",
    "react-router-dom": "^5.1.2",
    "react-scripts": "3.2.0"
  },
  "scripts": {
    "start": "BROWSER=none FORCE_COLOR=yes react-app-rewired start | cat",
    "build": "FORCE_COLOR=yes react-app-rewired build | cat",
    "test": "FORCE_COLOR=yes react-app-rewired test | cat",
    "eject": "FORCE_COLOR=yes react-app-rewired eject | cat",
    "test:cypress": "cypress open",
    "test:integration": "yarn build && concurrently --kill-others \"../backend/scripts/staging.sh\" \"cypress run\"",
    "test": "yarn test:integration"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "cypress": "^3.7.0",
    "react-app-rewire-disable-chunks": "^0.0.1",
    "react-app-rewired": "^2.1.5"
  }
}
